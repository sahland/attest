name: attest_example
description: Intentionally-broken demo app used to dogfood attest in CI.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.8.0
  flutter: ">=3.32.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  attest_flutter: ^1.0.0
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
